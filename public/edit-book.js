// Run once the document has loaded all html elements.
$(function() {
    // ==========================================
    // ===          Initialize Book           ===
    // ==========================================

    let url = window.location.search;
    
    // ==========================================
    // ===            Editing Books           ===
    // ==========================================
    
    
});